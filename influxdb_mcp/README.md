# Home Assistant InfluxDB MCP

This folder is the Home Assistant app (formerly add-on). See [DOCS.md](DOCS.md) for configuration and connection instructions.

The app is read-only. It generates bounded InfluxQL itself and does not expose a raw-query tool.
